{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "echo 'Skipping static build - Metro builds on demand'",
    "variables": {
      "NODE_VERSION": "20.18.1"
    }
  },
  "deploy": {
    "startCommand": "cd artifacts/techno-rogue && CI=1 npx expo start --web --port $PORT --non-interactive",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}
