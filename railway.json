{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "mkdir -p lib/integrations && pnpm install --frozen-lockfile && cd artifacts/techno-rogue-mobile && npx expo export --platform web"
  },
  "deploy": {
    "startCommand": "cd artifacts/techno-rogue-mobile && CI=1 npx expo start --non-interactive",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}
